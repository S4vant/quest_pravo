export const gameState = {
    taskStartedAt: null,
    taskUnlocked: false,
    timerInterval: null,
    timerInterval: null,
    draggedBlock: null,
};
export const IS_MOBILE = 'ontouchstart' in window || navigator.maxTouchPoints > 0;